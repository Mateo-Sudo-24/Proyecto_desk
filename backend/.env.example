# =========================
# Ejemplo de archivo .env
# =========================

# --- Configuración de la base de datos SQL Server ---

# Para una instancia estándar de SQL Server:
# DATABASE_URL="sqlserver://USUARIO:CONTRASEÑA@localhost:1433;database=NOMBRE_BD;trustServerCertificate=true;encrypt=false"

# Para una instancia LocalDB (solo si usas LocalDB):
# DATABASE_URL="sqlserver://NOMBRE_INSTANCIA;database=NOMBRE_BD;user=USUARIO;password=CONTRASEÑA;trustServerCertificate=true;encrypt=false"
# Ejemplo: DATABASE_URL="sqlserver://User\\LOCALDB#C19A7D40;database=EcuatechnologyDB;user=sa;password=12345678;trustServerCertificate=true;encrypt=false"

# --- Configuración del portal del cliente (para enlaces en emails) ---
CLIENT_PORTAL_URL="http://localhost:3000"

# --- Configuración de correo (Nodemailer) ---
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="tu_email@gmail.com"
SMTP_PASS="tu_contraseña_o_app_password"

# --- JWT (para autenticación) ---
JWT_SECRET="tu_clave_secreta"
JWT_EXPIRES_IN="24h"

# --- Otros parámetros opcionales ---
NODE_ENV="development"
PORT=4000

# --- Parámetros de seguridad ---
BCRYPT_ROUNDS=12
OTP_EXPIRY_MINUTES=15

# --- Configuración de subida de archivos ---
MAX_FILE_SIZE=5242880
UPLOAD_PATH="./uploads"

# =========================
# Instrucciones:
# 1. Copia este archivo como .env en la raíz del backend.
# 2. Completa los valores según tu entorno.
# 3. Guarda y ejecuta los comandos npm/prisma normalmente.
# =========================